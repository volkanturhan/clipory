<Window x:Class="Clipory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Clipory"
        xmlns:services="clr-namespace:Clipory.Services"
        mc:Ignorable="d"
        Title="Clipory"
        Width="380" Height="460"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="Manual"
        FontFamily="Segoe UI"
        Deactivated="OnDeactivated">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <!-- Brand + neutral palette, shared across the popup. -->
        <SolidColorBrush x:Key="Accent" Color="#4F46E5" />
        <SolidColorBrush x:Key="Surface" Color="#FFFFFF" />
        <SolidColorBrush x:Key="Subtle" Color="#F3F4F6" />
        <SolidColorBrush x:Key="Border" Color="#E5E7EB" />
        <SolidColorBrush x:Key="TextPrimary" Color="#111827" />
        <SolidColorBrush x:Key="TextMuted" Color="#6B7280" />

        <!-- Search box: a soft, rounded, borderless field. -->
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource Subtle}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="CaretBrush" Value="{StaticResource Accent}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- History rows: rounded, with hover and an accent-filled selection. -->
        <Style x:Key="ClipItemStyle" TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Margin" Value="0,1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" CornerRadius="6"
                                Background="Transparent"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Subtle}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Accent}" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Outer margin leaves room for the drop shadow; the card is the Border. -->
    <Border Margin="12" CornerRadius="12"
            Background="{StaticResource Surface}"
            BorderBrush="{StaticResource Border}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.22"
                              BlurRadius="20" ShadowDepth="0" />
        </Border.Effect>

        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header: brand mark + live item count. -->
            <Grid Grid.Row="0" Margin="2,0,2,10">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="14" Height="14" CornerRadius="4"
                            Background="{StaticResource Accent}" />
                    <TextBlock Text="Clipory" Margin="8,0,0,0"
                               FontWeight="SemiBold" FontSize="13"
                               Foreground="{StaticResource TextPrimary}"
                               VerticalAlignment="Center" />
                </StackPanel>
                <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center"
                           FontSize="11" Foreground="{StaticResource TextMuted}"
                           Text="{Binding Items.Count, ElementName=HistoryList}" />
            </Grid>

            <!-- Search field with a placeholder that shows only while empty. -->
            <Grid Grid.Row="1" Margin="0,0,0,8">
                <TextBox x:Name="SearchBox"
                         Style="{StaticResource SearchBoxStyle}"
                         KeyDown="OnSearchKeyDown"
                         TextChanged="OnSearchTextChanged" />
                <TextBlock Text="{Binding [SearchPlaceholder], Source={x:Static services:Localization.Instance}}"
                           IsHitTestVisible="False"
                           Margin="12,0,0,0" VerticalAlignment="Center"
                           FontSize="13" Foreground="{StaticResource TextMuted}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <!-- The history list, plus an empty-state message behind it. -->
            <Grid Grid.Row="2">
                <ListBox x:Name="HistoryList"
                         Background="Transparent" BorderThickness="0"
                         HorizontalContentAlignment="Stretch"
                         ItemContainerStyle="{StaticResource ClipItemStyle}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         MouseDoubleClick="OnListDoubleClick">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding Preview}"
                                           TextTrimming="CharacterEllipsis"
                                           VerticalAlignment="Center" />

                                <!-- Amber star marks pinned clips. -->
                                <TextBlock Grid.Column="1" Text="★" Margin="8,0,0,0"
                                           VerticalAlignment="Center" Foreground="#F59E0B"
                                           Visibility="{Binding IsPinned, Converter={StaticResource BoolToVis}}" />

                                <Grid.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="{Binding [MenuPin], Source={x:Static services:Localization.Instance}}"
                                                  Click="OnPinClick" />
                                        <MenuItem Header="{Binding [MenuDelete], Source={x:Static services:Localization.Instance}}"
                                                  Click="OnDeleteClick" />
                                    </ContextMenu>
                                </Grid.ContextMenu>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <TextBlock Text="{Binding [EmptyState], Source={x:Static services:Localization.Instance}}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontSize="12" Foreground="{StaticResource TextMuted}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasItems, ElementName=HistoryList}" Value="False">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <!-- Keyboard hint. -->
            <TextBlock Grid.Row="3" Margin="2,10,2,0"
                       FontSize="11" Foreground="{StaticResource TextMuted}"
                       TextWrapping="Wrap"
                       Text="{Binding [Hint], Source={x:Static services:Localization.Instance}}" />
        </Grid>
    </Border>
</Window>
